%% -*- mode: erlang;erlang-indent-level: 4;indent-tabs-mode: nil -*-

%%
%% This test configuration is used for testing escalus
%% against a running MongooseIM server

%% the Erlang node name of tested ejabberd/MongooseIM
{ejabberd_node, 'mongooseim@localhost'}.
{ejabberd_cookie, ejabberd}.

%% the main XMPP domain served by the tested ejabberd/MongooseIM
{ejabberd_domain, <<"localhost">>}.

%% the server host name or ip addres
{ejabberd_addr, <<"127.0.0.1">>}.
{ejabberd_secondary_domain, <<"localhost.bis">>}.
{ejabberd_metrics_rest_port, 5280}.
{ejabberd_string_format, bin}.
{escalus_user_db, {module, escalus_ejabberd, []}}.

%% Log all stanzas sent and received (consumed) in the test cases
%% This can be very useful, but it is also very noisy and some test suites
%% can cause it to choke. Use sparingly and with caution.
%% {stanza_log, true}.

{escalus_users, [
    {alice, [
        {username, <<"043e8c96-ba30-11e5-9912-ba0be0483c18">>},
        {server, <<"localhost">>},
        {password, <<"makota">>},
        {compression, <<"zlib">>}
    ]},
    {bob, [
        {username, <<"2396aa72-c5fe-11e5-9912-ba0be0483c18">>},
        {server, <<"localhost">>},
        {password, <<"makrolika">>},
        {starttls, optional}
    ]},
    {carol, [
        {username, <<"1e6f3b36-c5fe-11e5-9912-ba0be0483c18">>},
        {server, <<"localhost">>},
        {password, <<"uber-secret">>},
        {starttls, optional}
    ]},
    {karen, [
        {username, <<"2841ef1e-c5fe-11e5-9912-ba0be0483c18">>},
        {server, <<"localhost">>},
        {password, <<"makrowe;p">>}
    ]},
    {tim, [
        {username, <<"31a07b20-c5fe-11e5-9912-ba0be0483c18">>},
        {server, <<"localhost">>},
        {password, <<"blablah">>}
    ]},

    {alice_sm, [
        {username, <<"27cefff6-b9ae-11e5-b7ee-080027f70e96">>},
        {server, <<"localhost">>},
        {password, <<"super-secret">>},
        {stream_management, true}
        %% {manual_ack, true} %% uncomment to prevent automatic
                              %% ack sending by escalus_tcp

    ]},
    {bob_sm, [
        {username, <<"281373c0-b9ae-11e5-b37e-080027f70e96">>},
        {server, <<"localhost">>},
        {password, <<"hyper-secret">>},
        {stream_management, true}
    ]}
]}.

%% vim: ft=erlang

